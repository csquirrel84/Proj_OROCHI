---
# The orochi node IP is resolved dynamically by fuse.yml at startup.
# Do not add static hosts here — fuse.yml uses add_host to populate this group.
all:
  children:
    orochi_node:
      hosts: {}
